@model CardType

<turbo-stream action="remove" target="CardType_@Model.Id"></turbo-stream>

<turbo-stream action="replace" target="card-type-list-counter">
    <template>
        <partial name="_CardTypeListCounter.cshtml" model="@Model"/>
    </template>
</turbo-stream>