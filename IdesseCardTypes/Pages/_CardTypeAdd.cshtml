@model CardType

<turbo-stream action="replace" target="CardType_@Model.Id">
    <template>
        <turbo-frame id="CardType_@Model.Id"></turbo-frame>
    </template>
</turbo-stream>

<turbo-stream action="append" target="card-type-list-frame">
    <template>
        <partial name="_CardTypeView" model="@Model"/>
    </template>
</turbo-stream>